# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Functions Dependencies # <--- Make sure this line starts with ONLY 6 spaces before the '-'
        run: npm ci                       # <--- This line and 'working-directory' need 2 more spaces than '- name:'
        working-directory: functions/
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_RAJUKART_AE5CA }}
          channelId: live
          projectId: rajukart-ae5ca
